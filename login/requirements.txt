#pip install -r requirements.txt

pip install Flask
pip install Flask pymongo

Flask
pymongo[srv]
Flask-PyMongo
pip install Flask Flask-PyMongo Flask-Migrate
pip install Flask-MongoEngine
pip install Flask-Mail
